name: goldora_app
description: Luxury gold and jewelry storefront app.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.13.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  get: ^4.7.3
  dio: ^5.11.1
  flutter_secure_storage: ^11.0.0
  cached_network_image: ^4.0.0
  google_fonts: ^8.2.1
  intl: ^0.20.3

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
